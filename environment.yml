name: terrain_pipeline
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - gdal>=3.6.0             # core: used for geospatial data processing
  - numpy>=1.24.0           # core: numerical computations
  - matplotlib>=3.7.0       # core: data visualization
  - requests>=2.28.0        # core: HTTP requests for web data retrieval
  - pysheds>=0.3.3
  - geopandas>=1.1.2
  - pip
  - pip:

    - ruff
